import React, { useState } from 'react';
import styles from './index.scss';

const Home = () => {
  return (
    <div className={styles.test}>
      <p>Home</p>
    </div>
  );
};

export default Home;
